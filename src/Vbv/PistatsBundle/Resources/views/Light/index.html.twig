{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Light list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Light</th>
                <th>Time</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('light_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.light }}</td>
                <td>{{ entity.time }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('light_show', { 'id': entity.id }) }}">show</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {% endblock %}
